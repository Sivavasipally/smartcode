### C++ skill
- C++17/20: RAII everywhere, smart pointers over raw owning pointers.
- Prefer `std::string_view`/`span` params, `constexpr`/`const` correctness.
- Rule of zero; delete or default special members explicitly when needed.
- No naked new/delete; use algorithms/range-for over index loops.
