### Java skill
- Java 17+ idioms: records for data, `var` for obvious locals, switch expressions.
- Favor immutability: final fields, unmodifiable collections, builder for complex types.
- Throw specific exceptions; never return null collections (return empty).
- One public type per file; package-by-feature naming.
