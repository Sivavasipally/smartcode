### Ruby skill
- Follow community style: snake_case, 2-space indent, guard clauses over nesting.
- Prefer keyword arguments for multi-param methods; frozen_string_literal comment.
- Use blocks/Enumerable (`map`, `select`, `each_with_object`) over manual loops.
- Raise specific StandardError subclasses.
