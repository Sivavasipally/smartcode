### C# skill
- .NET 8 idioms: file-scoped namespaces, records for DTOs, nullable reference types on.
- async/await end-to-end (`Task`-returning, `CancellationToken` params on public async APIs).
- Use LINQ judiciously; expression-bodied members for trivial accessors.
- Dependency injection via constructors; interfaces for seams.
