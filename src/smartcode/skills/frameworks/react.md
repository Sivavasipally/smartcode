### React skill
- Function components + hooks only; no class components.
- Props typed with an explicit interface; destructure in the signature.
- Derive state where possible; lift state up before reaching for context.
- Effects: narrow dependency arrays, cleanup functions, no async directly in useEffect.
- Keys stable and unique; avoid index-as-key for mutable lists.
